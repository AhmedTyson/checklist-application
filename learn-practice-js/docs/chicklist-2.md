**Ultimate Master List** التي لا تترك صغيرة ولا كبيرة في لغة JavaScript وتنقل تفكيرك لمستوى الـ Standard العالمي للشركات، قمت بتجهيز قائمة تضم **110 نقطة (Checklist)**.

هذه القائمة مصممة خصيصاً بعقلية المبرمج الذي يريد إتقان الواجهات الأمامية (Front-end) لربطها باحترافية مع خلفية برمجية قوية.

إليك القائمة الأضخم، جاهزة للنسخ إلى **Notion** لتبدأ في تتبع إنجازك:

### ⚙️ المرحلة الأولى: إعداد البيئة والأساسيات (Environment & Basics)

* [ ] 1. تثبيت Node.js وإعداد VS Code.
* [ ] 2. تثبيت إضافات ESLint و Prettier لضمان كود نظيف (Clean Code).
* [ ] 3. ربط ملف `app.js` بملف `index.html` وتشغيله في المتصفح.
* [ ] 4. الطباعة في الـ Console (`console.log`, `console.error`, `console.table`).
* [ ] 5. إدراك الفرق الجوهري بين `var` و `let` و `const`.
* [ ] 6. فهم نطاق المتغيرات: Block Scope مقابل Function Scope.
* [ ] 7. أنواع البيانات الأساسية (String, Number, Boolean).
* [ ] 8. الفرق الدقيق بين `null` و `undefined` و `NaN`.
* [ ] 9. استخدام Template Literals (علامات التنصيص المائلة ```).
* [ ] 10. المعاملات الحسابية (Arithmetic Operators).
* [ ] 11. معاملات المقارنة والفرق بين `==` و `===` (Strict Equality).
* [ ] 12. المعاملات المنطقية (`&&`, `||`, `!`).
* [ ] 13. استخدام المعامل الحديث Nullish Coalescing (`??`).
* [ ] 14. الجمل الشرطية `if`, `else if`, `else`.
* [ ] 15. استخدام `switch` cases.
* [ ] 16. اختصار الشروط باستخدام Ternary Operator (`? :`).
* [ ] 17. حلقات التكرار الأساسية (`for`, `while`, `do...while`).

### 📦 المرحلة الثانية: هيكلة البيانات (Objects & Arrays)

* [ ] 18. إنشاء كائن (Object Literal).
* [ ] 19. الوصول لخصائص الكائن (Dot notation vs Bracket notation).
* [ ] 20. إضافة، تعديل، وحذف خصائص الكائن.
* [ ] 21. الدوال داخل الكائنات (Object Methods).
* [ ] 22. تفكيك الكائنات (Object Destructuring).
* [ ] 23. دمج الكائنات باستخدام Spread Operator (`...`).
* [ ] 24. التسلسل الآمن Optional Chaining (`?.`) لتجنب أخطاء الـ Null.
* [ ] 25. إنشاء المصفوفات (Arrays).
* [ ] 26. تفكيك المصفوفات (Array Destructuring).
* [ ] 27. نسخ ودمج المصفوفات بالـ Spread Operator.
* [ ] 28. إضافة وإزالة عناصر (`push`, `pop`, `shift`, `unshift`).
* [ ] 29. استقطاع المصفوفات (`slice` vs `splice`).

### 🛠️ المرحلة الثالثة: دوال المصفوفات المتقدمة (Functional Programming)

*(هذا الجزء هو الأهم لمعالجة البيانات القادمة من قواعد البيانات)*

* [ ] 30. التكرار باستخدام `forEach`.
* [ ] 31. تحويل البيانات باستخدام `map`.
* [ ] 32. تصفية البيانات باستخدام `filter` (مثال: تصفية منتجات متجر سيراميك).
* [ ] 33. تجميع البيانات باستخدام `reduce` (مثال: حساب إجمالي سلة المشتريات).
* [ ] 34. البحث عن عنصر `find` و `findIndex`.
* [ ] 35. التحقق من الشروط باستخدام `some` و `every`.
* [ ] 36. ترتيب المصفوفات `sort` (وفهم مشكلة الترتيب الأبجدي للأرقام).
* [ ] 37. التحقق من وجود عنصر `includes`.
* [ ] 38. سلسلة الدوال (Chaining Methods مثل `.filter().map()`).

### 🧩 المرحلة الرابعة: الدوال (Functions & Logic)

* [ ] 39. كتابة الدوال التقليدية (Function Declarations).
* [ ] 40. الدوال كمتغيرات (Function Expressions).
* [ ] 41. إتقان الـ Arrow Functions (`() => {}`).
* [ ] 42. الإرجاع الضمني (Implicit Return) في الـ Arrow Functions.
* [ ] 43. المعاملات الافتراضية (Default Parameters).
* [ ] 44. تجميع المعاملات (Rest Parameters `...args`).
* [ ] 45. فهم سلوك كلمة `this` داخل الدوال العادية.
* [ ] 46. فهم سلوك كلمة `this` داخل الـ Arrow Functions (الفرق الجوهري).
* [ ] 47. التحكم في `this` باستخدام `call`, `apply`, `bind`.

### 🖥️ المرحلة الخامسة: السيطرة على المتصفح (DOM Manipulation)

* [ ] 48. فهم شجرة الـ DOM (Document Object Model).
* [ ] 49. جلب العناصر بـ `getElementById`.
* [ ] 50. جلب العناصر بـ `querySelector` و `querySelectorAll`.
* [ ] 51. تغيير النصوص (`textContent` vs `innerText` vs `innerHTML`).
* [ ] 52. تغيير ستايل الـ CSS عبر جافا سكريبت (`element.style`).
* [ ] 53. التحكم في الـ Classes عبر (`classList.add`, `remove`, `toggle`).
* [ ] 54. إنشاء عناصر HTML جديدة (`document.createElement`).
* [ ] 55. إضافة العناصر للمتصفح (`append`, `appendChild`).
* [ ] 56. إدراج العناصر في أماكن محددة (`insertAdjacentHTML`).
* [ ] 57. حذف العناصر من المتصفح (`remove()`).
* [ ] 58. التنقل في شجرة الـ DOM (`parentNode`, `children`, `nextElementSibling`).
* [ ] 59. التعامل مع خصائص البيانات المخصصة (HTML `data-` attributes).

### 🖱️ المرحلة السادسة: التفاعل والأحداث (Events)

* [ ] 60. إضافة مستمع للأحداث `addEventListener`.
* [ ] 61. التعامل مع أحداث الماوس (`click`, `mouseenter`, `mouseleave`).
* [ ] 62. التعامل مع أحداث لوحة المفاتيح (`keydown`, `keyup`).
* [ ] 63. التعامل مع أحداث النماذج والإدخال (`input`, `change`, `focus`).
* [ ] 64. إرسال النماذج (`submit`).
* [ ] 65. منع السلوك الافتراضي للمتصفح (`e.preventDefault()`).
* [ ] 66. فهم كائن الحدث (Event Object).
* [ ] 67. الفرق بين `e.target` و `e.currentTarget`.
* [ ] 68. إدراك مفهوم فوران الأحداث (Event Bubbling).
* [ ] 69. التقاط الأحداث (Event Capturing).
* [ ] 70. إتقان نمط تفويض الأحداث (Event Delegation) لتحسين الأداء.

### ⏳ المرحلة السابعة: البرمجة غير المتزامنة (Asynchronous JS & APIs)

* [ ] 71. فهم الفرق بين الكود المتزامن وغير المتزامن.
* [ ] 72. تأخير تنفيذ الكود `setTimeout`.
* [ ] 73. تكرار تنفيذ الكود `setInterval` وإيقافه بـ `clearInterval`.
* [ ] 74. دوال الـ Callbacks وفهم مشكلة (Callback Hell).
* [ ] 75. التعرف على الـ Promises لحل مشكلة الـ Callbacks.
* [ ] 76. حالات الـ Promise الثلاث (Pending, Fulfilled, Rejected).
* [ ] 77. استهلاك الـ Promises باستخدام `.then()` و `.catch()`.
* [ ] 78. استخدام `Promise.all()` لتنفيذ عدة طلبات في نفس الوقت.
* [ ] 79. إتقان الأسلوب الحديث `async` و `await`.
* [ ] 80. اصطياد الأخطاء باستخدام `try...catch` blocks.
* [ ] 81. جلب البيانات من الـ Server باستخدام `fetch()`.
* [ ] 82. تحويل استجابة السيرفر إلى JSON (`response.json()`).
* [ ] 83. إرسال بيانات (POST requests) مع تخصيص الـ Headers.
* [ ] 84. التعامل مع أكواد استجابة الـ HTTP (مثل 200, 404, 500).

### 🧠 المرحلة الثامنة: ما تحت الغطاء (Under The Hood)

*(هنا يكمن الفرق بين المبتدئ والمحترف)*

* [ ] 85. كيف يعمل محرك V8.
* [ ] 86. فهم بيئة التنفيذ (Execution Context).
* [ ] 87. مكدس الاستدعاءات (The Call Stack).
* [ ] 88. ظاهرة الرفع (Variable Hoisting) وكيف تتجنبها.
* [ ] 89. رفع الدوال (Function Hoisting).
* [ ] 90. سلسلة النطاقات (The Scope Chain).
* [ ] 91. الفهم العميق للـ Closures (أكثر سؤال متكرر في الـ Interviews).
* [ ] 92. إدارة الذاكرة وجامع القمامة (Garbage Collection).
* [ ] 93. حلقة الأحداث (The Event Loop) والـ Task Queue.
* [ ] 94. التمييز بين Microtasks والـ Macrotasks.

### 🏗️ المرحلة التاسعة: البرمجة الكائنية الموجهة (OOP in JS)

* [ ] 95. الدوال البانية (Constructor Functions).
* [ ] 96. الوراثة النمطية (Prototypal Inheritance).
* [ ] 97. استخدام الكلاسات الحديثة `class` و `constructor`.
* [ ] 98. الوراثة بين الكلاسات باستخدام `extends` و `super()`.
* [ ] 99. تغليف البيانات (Encapsulation) والخصائص الخاصة (Private fields `#`).
* [ ] 100. الـ Getters والـ Setters.
* [ ] 101. الدوال الثابتة (Static Methods).

### 🚀 المرحلة العاشرة: أدوات وتطبيقات متقدمة (Real-world Tools)

* [ ] 102. تنظيم الكود في ملفات باستخدام ES6 Modules (`import` / `export`).
* [ ] 103. التخزين المحلي `localStorage` لحفظ بيانات المستخدم.
* [ ] 104. التخزين المؤقت `sessionStorage`.
* [ ] 105. تحويل البيانات `JSON.parse` و `JSON.stringify`.
* [ ] 106. التعامل مع التواريخ باستخدام كائن `Date`.
* [ ] 107. العمليات الرياضية المتقدمة بكائن `Math`.
* [ ] 108. التعابير النمطية الأساسية (Regular Expressions) للتحقق من المدخلات (Validation).
* [ ] 109. إنشاء أخطاء مخصصة (Custom Errors) بـ `throw new Error()`.
* [ ] 110. استخدام هياكل بيانات `Set` (للقيم الفريدة) و `Map`.

